export enum TransactionType {
  INCOME = 'INCOME',
  EXPENSE = 'EXPENSE'
}

export enum DebtDirection {
  OWE = 'OWE',
  OWED_TO_ME = 'OWED_TO_ME'
}

export enum DebtStatus {
  PENDING = 'PENDING',
  PAID = 'PAID'
}
